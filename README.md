# CI-CD-TEST

A tiny Node.js and Express app you can use to learn CI/CD pipelines.

## Routes

- `GET /` returns a welcome response
- `GET /health` returns a health check response

## Run locally

```bash
npm install
npm start
```

## Test

```bash
npm test
```
